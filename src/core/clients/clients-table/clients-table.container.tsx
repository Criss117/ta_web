interface Props {
  page: number;
  offset: number;
  query?: string;
}

const ClientsTableContainer = ({ offset, page, query }: Props) => {
  return <div>ClientsTableContainer</div>;
};

export default ClientsTableContainer;
